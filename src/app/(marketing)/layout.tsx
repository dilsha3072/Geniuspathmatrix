
'use client';

// This layout has been deprecated in favor of a single, unified root layout.
export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
