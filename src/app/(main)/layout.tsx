
'use client';

// This layout is no longer needed as the root layout now handles the sidebar.
export default function MainLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
